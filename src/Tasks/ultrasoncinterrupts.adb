-- Attempt at getting a working interrupt based ultrasonic readout

--with MicroBit.TimeHighspeed; use MicroBit.TimeHighspeed;
--with Ada.Interrupts; use Ada.Interrupts;
--with MicroBit.IOsForTasking; use MicroBit.IOsForTasking;
--with NRF_SVD.GPIO; use NRF_SVD.GPIO;
--use MicroBit;
--with HAL; use HAL;

--package body ultrasoncinterrupts is
 --  protected ultrainterrupts is
  --    procedure frontinterrupt
   --   pragma Interrupt_Handler(ultrainterrupts);
   
     -- begin
      --   Attach_Handler(ultrainterrupts.frontinterrupt'Access, 
--end ultrasoncinterrupts;
